// Type declarations for `wasix-suite.constants.mjs`. Kept beside the
// `.mjs` so TypeScript consumers (the Playwright spec, the typed facade
// in `wasix-suite.config.ts`) see a typed module rather than `any`.

export declare const WASMER_SHA: string;
export declare const WASIX_INCLUDE_DIRS: readonly string[];
export declare const WASIX_VENDOR_DIR: string;
export declare const WASIX_SUITE_BIN_DIR: string;
